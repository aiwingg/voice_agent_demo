{
  "name": "voice-agent-demo",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://sycorax.ai/demo",
  "scripts": {
    "sync-config": "node sync-config.js",
    "prestart": "npm run sync-config",
    "start": "node server.js",
    "build:client": "npm run sync-config && cd client && npm run build",
    "heroku-postbuild": "npm run sync-config && cd client && npm install && npm run build"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "googleapis": "^129.0.0",
    "retell-sdk": "your-retell-sdk-version"
  }
}
